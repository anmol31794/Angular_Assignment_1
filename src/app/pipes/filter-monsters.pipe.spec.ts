import { FilterMonstersPipe } from './filter-monsters.pipe';

describe('FilterMonstersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMonstersPipe();
    expect(pipe).toBeTruthy();
  });
});
